<%= form_for(notice) do |f| %>
  <% if notice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(notice.errors.count, "error") %> prohibited this notice from being saved:</h2>

      <ul>
      <% notice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %>
    <%= f.text_field :titulo %>
  </div>

  <div class="field">
    <%= f.label :corpo %>
    <%= f.text_area :corpo %>
  </div>

  <div class="field">
    <%= f.label :criacao %>
    <%= f.date_select :criacao %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
